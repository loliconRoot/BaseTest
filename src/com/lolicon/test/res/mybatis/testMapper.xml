<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.lolicon.test.mybatis.Test">
    <select id="test1" resultType="Logs">
        select * from test.logs limit 1
    </select>
    <select id="test2" resultType="Var">
        select * from test.var limit 1
    </select>
</mapper>
